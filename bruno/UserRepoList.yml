info:
  name: UserRepoList
  type: http
  seq: 11

http:
  method: GET
  url: https://api.github.com/users/NutNaphop/repos?sort=updated&order=desc
  headers:
    - name: Accept
      value: application/vnd.github+json
    - name: X-GitHub-Api-Version
      value: 2026-03-10
  params:
    - name: sort
      value: updated
      type: query
    - name: order
      value: desc
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
